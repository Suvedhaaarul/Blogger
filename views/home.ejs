<%- include("partials/header") %>

<div class="container">
  <h1 class="main-title">Welcome to this Blog</h1>

  <a class="write-btn" href="/post">➕ Write a New Blog</a>

  <% if (blog && blog.length > 0) { %>
    <% blog.forEach(b => { %>
      <div class="blog-post">
        <h2 class="blog-title"><%= b.title %></h2>
        <p class="blog-content"><%= b.content %></p>

        <div class="action-buttons">
          <form action="/edit/<%= b.id %>" method="POST">
            <button type="submit" class="btn edit">✏️ Edit</button>
          </form>

          <form action="/delete/<%= b.id %>" method="POST">
            <button type="submit" class="btn delete">🗑️ Delete</button>
          </form>
        </div>
      </div>
    <% }) %>
  <% } else { %>
    <p class="no-blogs">No blogs yet. Be the first to write!</p>
  <% } %>
</div>

<%- include("partials/footer") %>
